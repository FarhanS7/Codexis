// Root page renders the Codexis landing page.
// Authenticated users can navigate to /dashboard via the CTA buttons.
// The redirect-to-dashboard behavior lives in the (protected) layout guard.
export { default } from './(landing)/page';
